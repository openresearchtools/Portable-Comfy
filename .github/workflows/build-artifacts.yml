name: Build portable artifacts

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: portable-artifacts-${{ github.ref }}
  cancel-in-progress: false

env:
  COMFY_VERSION: 0.27.0
  COMFY_TAG: v0.27.0
  FRONTEND_VERSION: 1.45.20
  PYTHON_VERSION: 3.13.12
  TORCH_VERSION: 2.12.0+cu130
  TORCHVISION_VERSION: 0.27.0+cu130
  TORCHAUDIO_VERSION: 2.11.0+cu130
  CUDA_VERSION: "13.0"

jobs:
  build:
    name: Build CUDA 13 portable packages
    # Building on the oldest supported userspace keeps CPython, the launcher,
    # and AppImage native symbols compatible with Ubuntu 22.04 / glibc 2.35.
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      # A stock hosted runner starts with roughly 14 GB free. The portable
      # CPython, QtWebEngine and CUDA wheel trees need more room while both
      # the staging tree and final archive exist.
      - name: Reclaim hosted-runner disk space
        shell: bash
        run: |
          before="$(df -h /)"
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/.ghcup \
            /opt/hostedtoolcache/CodeQL
          sudo apt-get clean
          rm -rf "$HOME/.cache/pip"
          printf '%s\n' "$before"
          df -h /

      - name: Set up Python 3.13.12 build interpreter
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: 3.13.12

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            build-essential \
            curl \
            desktop-file-utils \
            file \
            git \
            libbz2-dev \
            libdb-dev \
            libexpat1-dev \
            libffi-dev \
            libgdbm-compat-dev \
            libgdbm-dev \
            liblzma-dev \
            libncursesw5-dev \
            libnsl-dev \
            libreadline-dev \
            libsqlite3-dev \
            libssl-dev \
            patchelf \
            pkg-config \
            rsync \
            squashfs-tools \
            tk-dev \
            uuid-dev \
            xz-utils \
            zlib1g-dev

      - name: Build complete portable archive
        shell: bash
        run: |
          mkdir -p "$GITHUB_WORKSPACE/artifacts" "$RUNNER_TEMP/portable-build"
          scripts/build_portable.sh \
            --output-dir "$GITHUB_WORKSPACE/artifacts" \
            --work-dir "$RUNNER_TEMP/portable-build"

      - name: Build replaceable Core and frontend archive
        shell: bash
        run: |
          scripts/build_core_bundle.sh \
            --output-dir "$GITHUB_WORKSPACE/artifacts" \
            --work-dir "$RUNNER_TEMP/core-build"

      - name: Preflight generated packages
        shell: bash
        run: |
          scripts/preflight_portable.sh \
            artifacts/Portable-Comfy-linux-x86_64.tar.gz
          tar -tzf \
            artifacts/Portable-Comfy-core-v0.27.0.tar.gz \
            >/dev/null
          (cd artifacts && sha256sum -- *.tar.gz | tee SHA256SUMS)
          du -h artifacts/*
          df -h /

      # upload-artifact always supplies an artifact container. Compression
      # level zero avoids recompressing an already-compressed tarball.
      - name: Upload complete portable archive
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: Portable-Comfy-linux-x86_64
          path: |
            artifacts/Portable-Comfy-linux-x86_64.tar.gz
            artifacts/SHA256SUMS
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

      - name: Upload replaceable Core archive
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: Portable-Comfy-core-v0.27.0
          path: |
            artifacts/Portable-Comfy-core-v0.27.0.tar.gz
            artifacts/SHA256SUMS
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

  smoke-downloaded-artifact:
    name: Download and smoke-test artifact
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out smoke-test scripts
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Download complete portable artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: Portable-Comfy-linux-x86_64
          path: downloaded/full

      - name: Download replaceable Core artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: Portable-Comfy-core-v0.27.0
          path: downloaded/core

      - name: Verify downloaded archive checksums
        shell: bash
        run: |
          (cd downloaded/full && sha256sum --check --ignore-missing SHA256SUMS)
          (cd downloaded/core && sha256sum --check --ignore-missing SHA256SUMS)

      - name: Validate downloaded Core bundle
        shell: bash
        run: |
          core_stage="$RUNNER_TEMP/core-bundle"
          mkdir -p "$core_stage"
          tar -xzf downloaded/core/Portable-Comfy-core-v0.27.0.tar.gz \
            -C "$core_stage" --no-same-owner --no-same-permissions
          python3 scripts/verify_core_bundle.py "$core_stage"

      - name: Install headless desktop libraries
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            curl \
            file \
            gcc \
            libegl1 \
            libfontconfig1 \
            libgl1 \
            libnss3 \
            libopengl0 \
            libxcomposite1 \
            libxdamage1 \
            libxkbcommon-x11-0 \
            libxrandr2 \
            libxcb-cursor0 \
            libxcb-icccm4 \
            libxcb-keysyms1 \
            libxcb-randr0 \
            libxcb-render-util0 \
            libxcb-shape0 \
            libxcb-xinerama0 \
            libxcb-xfixes0 \
            xauth \
            xvfb

      - name: Preflight the downloaded payload
        run: |
          scripts/preflight_portable.sh \
            downloaded/full/Portable-Comfy-linux-x86_64.tar.gz

      - name: Launch ComfyUI and the frozen desktop shell
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
          QTWEBENGINE_DISABLE_SANDBOX: "1"
        run: |
          xvfb-run --auto-servernum --server-args='-screen 0 1280x800x24' \
            scripts/smoke_artifact.sh \
              downloaded/full/Portable-Comfy-linux-x86_64.tar.gz \
              --timeout 120 \
              --allow-no-gpu
